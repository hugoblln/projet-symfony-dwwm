<section class="banner-hero" style="background: center / cover url('{{ vich_uploader_asset(terrain) }}')">
<div class="banner-hero-header">
<h1 class="banner-hero-title">{{ terrain.nom }}</h1>
</div>
</section>